import { Button } from 'antd';
import styled from 'styled-components';

export const VarBtn = styled(Button)`
    margin-right: 5px;
`;

export const BtnsBox = styled.div`
  position: relative;
  width: 100%;
  display: flex;
`;
